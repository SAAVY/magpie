----------------------
Setup
----------------------

In order to create the database with the correct schema, please run the "install.sh" file.
Postgres needs to be installed for it to work

Requires:
$POSTGRES_ADMIN - Postgres admin username
$DBUSER - User who is going to be the database owner
$DBNAME - Name of the database to be created
$TABLENAME - Name of the table to be created

It will also output the location of the database

----------------------
Python library - Usage Notes
----------------------

DbUtils.add_value(url, metadata)
    - Adds a url and it's corresponding metadata, if it does not already exist, to the database

DbUtils.get_metadata(url)
    - Retrieves MagpieData for corresponding url, even if url is not present in the database